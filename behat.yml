default:
    suites:
        many-cages:
            contexts:
                - Brunty\BehatApiResponses\Context\ApiContext: ~
    extensions:
        Laracasts\Behat:
            # env_path: .env.behat
        Behat\MinkExtension:
            default_session: laravel
            laravel: ~